name: GUI Verification
on:
  push:
    paths:
      - "gui/**"
      - "scripts/python/gui_backend.py"
      - "scripts/verify_phase6.sh"
  pull_request:
    paths:
      - "gui/**"
      - "scripts/python/gui_backend.py"
      - "scripts/verify_phase6.sh"
  workflow_dispatch:

jobs:
  verify-gui:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify GUI
        run: bash scripts/verify_phase6.sh
